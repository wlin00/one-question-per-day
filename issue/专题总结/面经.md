**米哈游一面**：
```typescript
1、vue和react的区别
2、extend和原型继承的区别
3、tabs组件的下划线动画怎么做
4、Promise.All & Promise.Race实现
5、Promise实现
6、react hook用过哪些
7、react hook 的useEffect里如何监听上一次的状态
8、实现一个倒计时的自定义hook 🌟
9、webpack打包原理
10、fiber架构思想 🌟
11、工作中遇到的难题
12、对前端工具的看法
```

**米哈游二面**：
```typescript
1、个人技术栈和项目
2、为什么想换工作，对未来规划
3、对状态管理的看法，什么时候使用状态管理，什么时候适合eventbus、依赖注入、useContext
4、vuex实现原理
5、vue双向绑定原理
4、keep alive实现原理 🌟
5、怎么比较两个对象？（进阶版深拷贝， 考虑出现循环引用的对象）🌟
6、vue源代码 异步队列 & 组件更新 🌟
7、发布订阅和观察者模式的区别和使用场景 🌟
8、react hooks是怎么检查到依赖变化的？
9、强弱缓存
10、垃圾回收机制
```

**拼多多一面**：
```typescript
1、个人技术栈和项目
2、vue-router有哪些模式
3、微前端怎么处理路由嵌入
4、微前端的JS、CSS隔离如何实现
5、Vue生命周期（8个基础的 + activated + deactivated）、父子组件的生命周期
4、vue双向绑定原理
5、手写Vue源码的Observer、Watcher类
6、手写TS类型体操 MyPick 方法 （Type-challenge🌟）
7、用哪些方式进行状态管理
8、webpack写过哪些loader和plugin
9、webpack有哪些打包阶段？Tabable有哪些hook
10、组件库如何实现css和js的按需加载
```

**字节本地生活一面**：
```typescript
1、各类类型判断，typeof 各种数据的返回值，为什么typeof null 等于object ✅
2、下面的代码输出什么？[1, 2] + [1, 2] 、 'a' + + 'b' ✅
3、bfc是什么，使用场景 & 如何触发，bfc原理是什么 🌟
4、vue生命周期
5、vue2和vue3的区别（双向绑定的区别、diff和静态节点优化、esm、composition-api，vue3新增weakmap的正负向缓存机制）
4、tree shaking原理 🌟
5、项目中的性能优化
6、讲讲项目
7、一道数据处理，将数组转为树结构
8、算法topK问题（数组中最高频的K的数，小顶堆解法 or 桶排序解法✅）
```

**成都字节基础架构一面**：
```typescript
1、自我介绍
2、技术栈
3、组件库挑一个组件讲讲（cascader）
4、vue响应式原理
5、watcher绑定是动态还是一开始就全绑定好的
6、看过webpack源码吗，说一下如何写一个打包器
8、算法：最长重复子数组
```

**成都字节基础架构二面**：
```typescript
1、自我介绍，敏捷小组的开发模式、业务线内前端如何分工、git分支管理和reivew代码
2、讲一讲项目的标签系统/交互式选品 & 组件为什么封装为三层 & 如何抽离自定义hook
3、vue 和 react区别
  1)  说一下react中为什么不能使用条件/循环语句，而vue组合式api可以；
  2)  聊下vue3的性能优化点: 
    diff算法的patchNodes的优化，将静态节点标记提升到render之外, 从而跳过patch的过程；
    Vue3将代码基于动态节点指令（if、for、slot）切割为嵌套区块来实现局部代码更新；
    Vue3的性能和模版大小解耦，和动态节点数量正相关等;
    Vue3将全局的API和内部的一些组件都通过ESM导出，在用户使用的时候能够按需imoort，这对webpack这类打包工具在打包的时候的Tree-shaking更加友好，可以将Dead Code通过Uglify或Terser这类插件来除去（尤大在2020年4月给出：vue2的代码运行时大小为22.5kb,Vue3则是13.5kb)
4、平时怎么学习前端
5、讲一下CSRF跨站请求伪造原理和防范
6、跨域如何解决
7、用Vue / React 写一个useCountDown的倒计时hook
8、说一下组件库项目，toast、popover的设计思路，做组件和做业务开发有什么差别
```

**快手商业化一面**：
```typescript
  1、组件库开发需要注意什么（答：按需、换肤等；换肤具体实现）
  2、实现过哪些组件，toast实现方式
  3、microapp的原理 
    & microapp 是如何加载子应用的 🌟
      答：
      1、基座以ajax的方式请求了子应用提供的url，获取到了子应用html，html里又写了后续的静态资源（做语法分析获取）
      2、获取到静态资源的字符串内容后，把返回的字符串内容进行eval（其中还做了沙箱和样式的隔离），挂到基座的microapp节点上实现子应用的加载渲染
    & 和iframe的区别 
    & 如何通信
  4、css模块化：BEM、CSS module、 scoped原理 & css in js方案 & 原子化css
  5、eventLoop & vue 异步队列 & nextTick的实现原理
  6、https加密过程
  7、vscode代码补全插件实现原理
  8、vue响应式原理 & diff算法 & vue模版编译
  9、手写：实现一个并发控制的任务队列
  10、手写promise.all
```

**Tiktok广告创意中心二面**：
```typescript
  1、自我介绍
  2、说项目
  3、微前端的js和css隔离是怎么实现
  4、做了一年多项目，对项目有哪些贡献；交互式选品组件如何拆分
  5、最近有在学什么新技术；目前的原子化css方案如tailwindcss、windicss、unocss等，相比之前的bootstrap有什么优势和区别
  6、说下自己的优势；做组件库对自己有哪些帮助；打开百度主页，说一下搜索框封存
  成组件要注意哪些点（说需要向外暴露一些接口、对外暴露一个v-model、历史搜索记录功能可以考虑使用vueuse的uselocalstorage来将响应式数据持久化等）
  7、聊了下开源大佬antfu，做开源的东西对自己有哪些帮助；
  8、看你面了个成都的部门，三面后挂了，觉得自己上一次有什么表现的不足的（自己说可能出在编程题，说了下二面面试官让用vue和react各实现一个倒计时自定义hook，只写了个vue的，react只是简述了下怎么实现，这可能导致面试官觉得能力存在短板）
  9、换工作原因，对下一个部门有什么憧憬（扯了下对广告业务也比较感兴趣，说了下对品牌广告和效果广告的理解）

  （打开编辑器，开始十多个问题）
  9、一个ul-li列表 怎么给li加点击监听
  10、如果要给某一个li标签去除监听怎么办？（阻止冒泡）如果只有某一个li不去除监听怎么办（且不能大量对需要去除监听的li使用阻止冒泡）
  11、上述场景，为什么在vue中给li加点击监听就行，而不用给ul加（vue源码对事件中心的初始化）
  12、实现一个函数，每隔一秒fetch一次（先实现一个sleep休眠然后递归调用fetch）；如果出现错误怎么捕获并中止程序；
  13、为什么用setTimeout而不是setinterval，底层有什么区别
  14、有三个异步请求，a、b、c，写一个函数让c在a、b执行结束之后执行；若不用promise和promise.all怎么做（观察者模式注册回调？）
  15、什么场景需要用微队列结合宏队列（扯了一下vue视图更新时的nexttick异步队列设计）
  16、看你说刷力扣，来做道题，设计一个微信抢红包算法，比如输入一个长度num等于5，输入一个总金额count等于5，输出一个长度等于红包个数的数组（先用递归实现，然后面试官说如何调整红包的公平性，想了想说根据第一个人抢的红包大小去和每个人平均值做一个对比，然后动态修改下一个人的可抢区间系数）
  17、面试官：上面那道题看你用了ts，但没有写返回的类型，那怎么给一个长度为5的整型数组上类型（类型体操，先写一个type方法获取整型数组长度，然后用工具Equal判断长度等于5）
  18、个人每次喜欢最后再问两个计算机基础问题，为什么数组的下标要从0开始；
  19、为什么js默认用数组来存储数据而不是链表；

  最后的问答环节；问了下广告部门那边是不是主要用react，然后大牛说不在乎候选人技术栈，他觉得vue学会的候选人可以在两周上手react。
```

**成都腾讯子公司一面**：
```ts
  1、实现一个dialog的class构造函数，内部有open和close方法，要求await open方法的时候会阻塞代码，然后调用close后解除阻塞
  2、js执行的底层原理，运行一个闭包函数，整个过程，执行上下文和调用栈是如何变化
  3、为什么加了overflow：auto的列表滚动会卡顿，从浏览器渲染的底层原理进行分析
  4、async和await是generator的语法糖，generator的实现原理
```